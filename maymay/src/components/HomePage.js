import { Button } from "atomize";
import React from 'react';


// Parent Component
const HomePage = () => {
  return (
    <div>
      <Button /> {/* Rendering the ChildComponent */}
    </div>
  );
};

export default HomePage;